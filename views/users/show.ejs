<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
</head>
<body>
    <h1> <%= profile.username %>'s  Profile</h1>

    <% if (profile.id === user.id) { %>
        <p>Go ahead and make changes to this user</p>
    <% } %>

</body>
</html>